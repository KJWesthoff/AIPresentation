# ── Claude Code ───────────────────────────────────────────────────────────────
.claude/
animations/mlp/.claude/
animations/mlp/.agents/
animations/mlp/skills-lock.json

# ── MNIST dataset (53 MB — see README for download instructions) ──────────────
MNIST_ORG/

# ── Generated files (reproducible via scripts — see README) ──────────────────
mnist-sample.js
mnist-sample.json
cnn-weights.js

# ── GloVe source + generated vectors (163 MB / 4.6 MB) ───────────────────────
# Source: https://nlp.stanford.edu/data/glove.6B.zip
# Regenerate: python3 generate-glove.py
glove.6B.50d.txt
glove-vectors.js

# ── Trained model weights ─────────────────────────────────────────────────────
*.pth

# ── Node / npm (624 MB — run: cd animations/mlp && npm install) ───────────────
node_modules/
animations/mlp/node_modules/

# ── Video files (large binaries — render locally via Remotion) ───────────────
*.mp4
*.mov
*.avi
*.mkv
*.webm

# ── Personal reference material (course notebooks, PII in filenames) ──────────
ReferenceMaterial/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# ── Jupyter ───────────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb_checkpoints

# ── OS metadata ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor / IDE ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
