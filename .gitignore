# ── Claude Code ───────────────────────────────────────────────────────────────
.claude/
animations/mlp/.claude/
animations/mlp/.agents/
animations/mlp/skills-lock.json

# ── Large source data (not needed to run the presentation) ───────────────────
MNIST_ORG/
mnist-sample.json

# ── GloVe source text (163 MB — regenerate with: python3 generate-glove.py) ──
glove.6B.50d.txt

# ── Trained model weights ─────────────────────────────────────────────────────
*.pth

# ── Node / npm (624 MB — run: cd animations/mlp && npm install) ───────────────
node_modules/
animations/mlp/node_modules/

# ── Video files (large binaries — render locally via Remotion) ───────────────
*.mp4
*.mov
*.avi
*.mkv
*.webm

# ── Personal reference material (course notebooks, PII in filenames) ──────────
ReferenceMaterial/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# ── Jupyter ───────────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb_checkpoints

# ── OS metadata ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor / IDE ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
